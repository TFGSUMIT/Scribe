# Personal Access Token (classic or fine-grained) with the `repo` and
# `project` scopes — `repo` manages labels/milestones/issues, `project`
# manages the GitHub Projects v2 board created by `bootstrap`.
GITHUB_TOKEN=
GITHUB_OWNER=
GITHUB_REPO=
PROJECT_NAME=